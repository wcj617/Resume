version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild:
        commands:
    build:
      commands: 
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: 
        - node_modules/**/*
